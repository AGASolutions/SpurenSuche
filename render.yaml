services:
  - type: web
    name: spurensuche-api
    runtime: python
    buildCommand: pip install .
    startCommand: person-scan-web example.config.json --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
